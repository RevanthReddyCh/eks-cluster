version: 3

projects:
  - dir: .
    workspace: destroy
    workflow: destroy-workflow

workflows:
  destroy-workflow:
    plan:
      steps:
        - run: terraform init -input=false
        - run: terraform plan -destroy -input=false -out=destroy.tfplan
    apply:
      steps:
        - run: terraform apply -input=false destroy.tfplan
